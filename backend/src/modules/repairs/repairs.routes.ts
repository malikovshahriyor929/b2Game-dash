import { Router } from "express"; import * as c from "./repairs.controller"; export const repairsRoutes=Router(); repairsRoutes.get("/",c.list); repairsRoutes.post("/",c.create); repairsRoutes.get("/:id",c.get); repairsRoutes.post("/:id/approve",c.approve); repairsRoutes.post("/:id/reject",c.reject); repairsRoutes.post("/:id/need-more-details",c.needMore); repairsRoutes.post("/:id/start-fixing",c.startFixing); repairsRoutes.post("/:id/mark-fixed",c.markFixed); repairsRoutes.post("/:id/confirm-fixed",c.confirmFixed); repairsRoutes.post("/:id/reject-fix",c.rejectFix);
