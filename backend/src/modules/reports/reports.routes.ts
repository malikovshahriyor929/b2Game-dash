import { Router } from "express"; import * as c from "./reports.controller"; export const reportsRoutes=Router(); reportsRoutes.get("/revenue",c.revenue); reportsRoutes.get("/shop-sales",c.shopSales); reportsRoutes.get("/product-profit",c.productProfit); reportsRoutes.get("/simulator-usage",c.simulatorUsage); reportsRoutes.get("/vip-usage",c.vipUsage); reportsRoutes.get("/repairs",c.repairs); reportsRoutes.get("/admin-activity",c.adminActivity); reportsRoutes.get("/shifts",c.shifts); reportsRoutes.get("/unpaid",c.unpaid); reportsRoutes.get("/inventory",c.inventory);
