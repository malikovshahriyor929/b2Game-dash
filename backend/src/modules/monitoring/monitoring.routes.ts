import { Router } from "express"; import { requireRole } from "../../middleware/role.middleware"; import * as c from "./monitoring.controller"; export const monitoringRoutes=Router(); monitoringRoutes.use(requireRole(["super_admin"])); monitoringRoutes.get("/overview",c.overview); monitoringRoutes.get("/branches",c.branches); monitoringRoutes.get("/admin-actions",c.adminActions); monitoringRoutes.get("/repair-timeline",c.repairTimeline); monitoringRoutes.get("/sales-live",c.salesLive); monitoringRoutes.get("/simulator-live",c.simulatorLive);
