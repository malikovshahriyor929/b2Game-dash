import { asyncHandler } from "../../utils/asyncHandler"; import { ok } from "../../utils/apiResponse"; import * as s from "./monitoring.service";
export const overview=asyncHandler(async(_req,res)=>ok(res,await s.overview())); export const branches=asyncHandler(async(_req,res)=>ok(res,await s.branches())); export const adminActions=asyncHandler(async(_req,res)=>ok(res,await s.adminActions())); export const repairTimeline=asyncHandler(async(_req,res)=>ok(res,await s.repairTimeline())); export const salesLive=asyncHandler(async(_req,res)=>ok(res,await s.salesLive())); export const simulatorLive=asyncHandler(async(_req,res)=>ok(res,await s.simulatorLive()));
